# velo sysctl deltas -- L1 (baseline). Appended to /etc/sysctl.conf.
# L1 keeps connectivity (IPv6 allowed); only the harmless source-route /
# redirect-accept knobs are tightened.  No forwarding.
net.inet.ip.forwarding=0
net.inet.ip.redirect=0
net.inet6.ip6.redirect=0
