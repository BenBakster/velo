# Gate A — спайк «hybrid boot» (загрузочный диск ВЕЗДЕ + FDE)

> **✅ DONE 2026-06-07: GREEN.** Спайк прогнан (SeaBIOS + OVMF в qemu грузятся с FDE-диска),
> фича **реализована** в коммите `29af128`. Этот файл сохранён как ПРОТОКОЛ — НЕ выполнять
> заново. Живым остаётся только остаточный caveat: `0xEF`-на-MBR-ESP не проверен на реальном
> Insyde/AMI UEFI (см. §внизу). Инструкции ниже — историческая запись, читать как архив.

> **(архив) НОВАЯ СЕССИЯ, ЧИТАЙ ЭТО ЦЕЛИКОМ, ПОТОМ ВЫПОЛНЯЙ.** Это **Gate A** фичи velo
> «hybrid boot-mode». Задача — за один проход в симуляторе (qemu) выяснить
> go/no-go: можно ли сделать **зашифрованный (FDE) внешний диск, который грузится
> и на legacy-BIOS, и на UEFI**. **Код фичи НЕ писать, пока этот спайк не вернёт GREEN.**
> Всё на throwaway-дисках; реальный nvme/внешние диски НЕ трогать. Команды — под `thx1138`.
> Стиль работы и ворота: см. память `feedback-vibe-coding-gates`, `project-velo-hybrid-boot`.
